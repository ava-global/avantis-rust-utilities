[package]
name = "avantis-utils-derive"
version = "0.1.0"
edition = "2021"
description = "Derive macro for avantis-utils crate"
license = "MIT"

[lib]
proc-macro = true

[dependencies]
syn = { version = "1.0", features = ["extra-traits"] }
quote = "1.0"
proc-macro2 = "1.0"
